<style rel="stylesheet" type="text/css">
  @import 'https://fonts.googleapis.com/css?family=Fira+Sans:300,300i,400,400i,500,500i,700,700i&subset=latin-ext';
  @import 'https://fonts.googleapis.com/css?family=Roboto+Mono:300,400&subset=latin-ext';
  @import 'https://cdnjs.cloudflare.com/ajax/libs/normalize/4.2.0/normalize.min.css';

  html, body { font-family: 'Fira Sans', 'Open Sans', sans-serif; }
  body { background: white; margin: 1em; line-height: 1.5; }
  h1, h2, h3, h4, h5, h6 { font-family: 'Roboto', sans-serif; }

  #main { position: fixed; top: 0; left: 0; right: 0; bottom: 0; overflow: hidden; display: flex; flex-direction: column; }
  .search { border: 0; outline: none; box-sizing: border-box; padding: 15px; width: 100%; font-size: 1.3em; background: #444; color: #EEE; height: 55px; }
  .wrapper { display: flex; flex-direction: row; max-height: calc(100vh - 55px); }

  #sidebar, #preview { max-height: 100%; overflow-y: auto; }
  #sidebar { flex: 1; }
  #preview { flex: 3.5; padding: 5% 8%; }

  .file-list-item { font-size: 0.9em; width: 100%; background: none; border: 0; text-align: left; padding: 1em; }
  .file-list-item:hover { background: #DDD; }
  .file-list-item.active { background: #CCC; }
  .file-name { font-weight: bold; display: block; }
  .file-contents { color: #777; }
  .item-count { font-size: 0.8em; position: absolute; top: 0; right: 0; color: #777; padding: 1em; }
  .warning { margin: auto; box-sizing: border-box; width: 90%; padding: 5%; background: rgba(200, 40, 40, 0.3); color: #AA2222; font-size: 0.9em; margin-top: 2em; }

  .footnote-definition-label { float:left; padding-right: 1em; font-size: 0.8em; }
  .footnote-definition-label:before { content: '['; }
  .footnote-definition-label:after { content: ']:'; }
</style>